/**
 * this should be a module that gets all the applied
 * styles. Probelm how to decide which style to apply.
 * With paragraph styles it is possible but with character styles not.
 * We remove some characters so the number of characters changes
 * We could hardcode some style but we cannot make it dynamically at this point
 *
 * Solutions could be:
 * create a dropdown where the user can decide.
 * @param  {[type]} arr [description]
 * @return {[type]}     [description]
 */
var style_getter = function(arr){
  var par_styles = [];
  var char_styles = [];
  for(var i = 0; i < arr.length;i++){
  }
};

